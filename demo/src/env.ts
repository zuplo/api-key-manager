// TODO - fix env
// export const PUBLIC_API_URL = getRequiredEnv("NEXT_PUBLIC_API_URL");

// function getRequiredEnv(key: string): string {
//   const value = process.env[key];
//   if (!value) {
//     throw new Error(`Missing required environment variable '${key}'`);
//   }
//   return value;
// }
